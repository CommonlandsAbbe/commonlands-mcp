{
  "name": "commonlands-mcp",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node scripts/deploy.mjs",
    "deploy:ci": "wrangler deploy --keep-vars --var ENVIRONMENT:production --var VERSION:$npm_package_version --var GIT_SHA:${GITHUB_SHA:-$(git rev-parse --short HEAD)}",
    "deploy:raw": "wrangler deploy",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "eval:routing": "node scripts/eval-mcp-routing.mjs",
    "verify": "npm run lint && npm run typecheck && npm test"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260428.0",
    "@eslint/js": "^9.39.1",
    "@typescript-eslint/eslint-plugin": "^8.46.3",
    "@typescript-eslint/parser": "^8.46.3",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.3",
    "vitest": "^4.0.14",
    "wrangler": "^4.43.0"
  },
  "engines": {
    "node": ">=22"
  }
}
